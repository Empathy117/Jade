# Immersive Reader Pipeline

This package will contain the build-time importer, director, matcher, and
playback compiler. Phase 1 only establishes an importable Python package and
its test entry point.
